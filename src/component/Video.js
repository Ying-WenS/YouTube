import React from "react";

const Video = ({ data }) => {
  return (
    <div className="video">
      <div id="controls">
        <button className="palyer_btn">
          <i class="fas fa-play"></i>
        </button>
      </div>
    </div>
  );
};

export default Video;
